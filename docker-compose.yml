version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      OPENCLAW_OPEN_CHAT_ON_START: "false"
    volumes:
      - ./config.yaml:/app/config.yaml
      - /var/log:/var/log:ro
    restart: unless-stopped
