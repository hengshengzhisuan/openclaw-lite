# OpenClaw Lite 项目排期及进度汇总

依据《OpenClaw Lite 技术产品说明书》：**§9 开发计划**为节奏，**§3–§8** 拆解任务。下列为主排期表（Markdown 表格：**需求 \| 功能 \| 子功能 \| 人员 \| 日期 \| 时长**）。

---

## 项目排期明细

| 需求 | 功能 | 子功能 | 人员 | 日期 | 时长 |
| --- | --- | --- | --- | --- | --- |
| 基础框架（说明书 §9 阶段一） | FastAPI 接入层 | 应用骨架、`host`/`port`、路由模块化；Uvicorn 启动说明（§2） | @后端 | 5月10日 | 0.5天 |
| 基础框架（说明书 §9 阶段一） | 健康检查 | `GET /health`：`status` / `version` / `uptime`（§8.2） | @后端 | 5月10日 | 0.2天 |
| 基础框架（说明书 §9 阶段一） | 统一执行接口 | `POST /execute`：`user_id`、`platform`、`message` 及响应体字段（§8.1） | @后端 | 5月10日 | 0.3天 |
| 基础框架（说明书 §9 阶段一） | LLM 客户端 | `provider` / `model` / `api_key` / `timeout`；兼容 OpenAI 及国内模型备选（§4.1、§5） | @后端 | 5月11日 | 0.5天 |
| 基础框架（说明书 §9 阶段一） | LLM 解析器 | 提示词模板：`browser \| database \| file \| api`、参数结构与示例映射（§4.1） | @后端 | 5月11日 | 0.5天 |
| 核心功能 · 一句话执行（§3.1） | 解析→执行链路 | 自然语言→解析→工具→格式化输出；示例订单量 `COUNT(*)` + 时间条件（§3.1） | @后端 | 5月11日 | 0.3天 |
| 工具实现（说明书 §9 阶段二） | 浏览器工具 | Playwright headless、导航超时、提取纯文本（§3.2 browser） | @后端 | 5月12日 | 0.8天 |
| 工具实现（说明书 §9 阶段二） | 数据库工具 | SQLAlchemy 只读 SQL；非 `SELECT` 拦截、`max_rows`/`timeout`/只读账号（§3.2 database） | @后端 | 5月12日 | 1天 |
| 工具实现（说明书 §9 阶段二） | 文件工具 | 白名单路径、`max_lines`、禁止写（§3.2 file） | @后端 | 5月13日 | 0.5天 |
| 工具实现（说明书 §9 阶段二） | API 工具 | 域名白名单、`timeout`、请求日志（§3.2 api） | @后端 | 5月13日 | 0.5天 |
| 工具实现（说明书 §9 阶段二） | 工具执行器公共逻辑 | `BaseTool`：`validate`→权限→超时执行→格式化输出→错误捕获（§4.2） | @后端 | 5月13日 | 0.3天 |
| 安全优化（说明书 §9 阶段三） | 输入层校验 | LLM 输出结构与危险负载校验（§4.3 输入层） | @后端 | 5月14日 | 0.3天 |
| 安全优化（说明书 §9 阶段三） | 工具层过滤 | 正则拦截 `DROP`/`DELETE`/高危类比指令（§4.3 工具层） | @后端 | 5月14日 | 0.3天 |
| 安全优化（说明书 §9 阶段三） | 审计日志 | 用户、工具、关键参数摘要、耗时、成功/失败（§4.3 审计层、§10.1） | @后端 | 5月14日 | 0.3天 |
| 安全优化（说明书 §9 阶段三） | 缓存（可选） | Redis：相同查询缓存 1 小时，降低 LLM 调用（§4.1） | @后端 | 5月15日 | 0.4天 |
| 配置管理（§5） | 配置文件 | `config.yaml`：`server`/`llm`/`database`/`file`/`api`/`im` 结构与占位符（§5.1） | @后端 | 5月15日 | 0.3天 |
| 配置管理（§5） | 环境变量 | `.env.example`：`OPENAI_API_KEY`、`FEISHU_*`、`WECHAT_*` 等清单（§5.2） | @后端 | 5月15日 | 0.2天 |
| IM 对接（说明书 §9 阶段四 · §3.3） | 飞书机器人 | 事件订阅回调、解密与 token、文本消息→`/execute`→回复格式化（§3.3） | @后端 | 5月16日 | 1天 |
| IM 对接（说明书 §9 阶段四 · §3.3） | 微信机器人 | 企业微信回调、消息接收与回复链路（与飞书共用执行层）（§3.3） | @后端 | 5月17日 | 1天 |
| IM 对接（说明书 §9 阶段四 · §3.3） | 消息呈现 | 表格/JSON/纯文本等与 IM 限制适配（§8.1） | @后端 | 5月17日 | 0.3天 |
| 部署方案（§6） | Docker 单机 | `Dockerfile`、`docker-compose`、只读挂载日志目录、`config.yaml` 卷（§6.1） | @后端、@运维 | 5月18日 | 0.5天 |
| 联调交付（说明书 §9 阶段五） | 双平台联调 | 飞书/微信各一条完整链路：@机器人 → 四工具抽样用例（§7.1–§7.4） | @后端、@运维 | 5月18日 | 0.5天 |
| 联调交付（说明书 §9 阶段五） | 接口与使用文档 | `POST /execute`、`GET /health`、错误码与示例 JSON；场景示例汇编（§8、§7） | @后端 | 5月19日 | 0.5天 |
| 运维监控（§10） | 日志与指标基线 | 日志格式对齐样例；QPS、响应时间、错误率、LLM 失败率阈值说明（§10.1–§10.2） | @后端、@运维 | 5月19日 | 0.3天 |
| 版本规划（§11） | V1.0 里程碑 | 四工具 + 双平台 + 基础安全 + 最小部署包可交付；阶段 1–10 天验收（§11 V1.0 / 约 2 周） | @后端、@运维 | 5月20日 | 验收节点 |
| 版本规划（§11） | V1.5（计划） | 工具链编排、结果导出为文件（§11 V1.5） | @后端 | 5月27日 | 3天 |
| 版本规划（§11） | V2.0（计划） | 上下文记忆、多轮对话（§11 V2.0） | @后端 | 6月10日 | 5天 |

---

## 进度汇总（§9 阶段）

| 阶段 | 说明书对应 | 计划区间（默认） | 关键产出 | 进度 |
| --- | --- | --- | --- | --- |
| 基础框架 | 第 1–2 天 | 5月10日–5月11日 | FastAPI、`/health`、`/execute`、LLM 与解析器 | 未开始 / 进行中 / 已完成 |
| 工具实现 | 第 3–4 天 | 5月12日–5月13日 | browser / database / file / api + 执行器骨架 | 未开始 / 进行中 / 已完成 |
| 安全优化 | 第 5–6 天 | 5月14日–5月15日 | 分层安全、审计日志、可选 Redis、配置定型 | 未开始 / 进行中 / 已完成 |
| IM 对接 | 第 7–8 天 | 5月16日–5月17日 | 飞书、企微触发与回复、消息格式化 | 未开始 / 进行中 / 已完成 |
| 联调交付 | 第 9–10 天 | 5月18日–5月20日 | Compose 部署、双平台测试、文档、运维监控、V1.0 里程碑 | 未开始 / 进行中 / 已完成 |
